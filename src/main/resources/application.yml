spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sharding?useSSL=false&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&serverTimezone=UTC
    username: root
    password: root

mybatis:
  mapper-locations: classpath*:/mapper/*.xml
  type-aliases-package: com.youyouxunyin.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.youyouxunyin.mapper:
      debug

